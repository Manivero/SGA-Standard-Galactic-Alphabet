// Переменные: immutable (let/const) vs mutable (var)
 pi = 3;
 max_players = 10;
 score = 0;
score = score + 5;
("pi =", pi);
("max_players =", max_players);
("score =", score);
